"""Reconciliation scripts for Agent Platform.

This package provides tools to reconcile on-chain blockchain state
with off-chain event store data, ensuring consistency between
the two systems.

Modules:
    config: Configuration and constants
    reconcile_payments: Payment verification reconciliation
    reconcile_delegations: Delegation state reconciliation
    reconcile_state_channels: State channel reconciliation
"""
